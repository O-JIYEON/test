const endpoints = {
  customers: '/api/customers',
  customerContacts: '/api/customer-contacts',
  leads: '/api/leads',
  deals: '/api/deals',
  activities: '/api/activity-logs',
  lookupCategories: '/api/lookup-categories',
  lookupValues: '/api/lookup-values',
  goals: '/api/goals'
};

export default endpoints;
